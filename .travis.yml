language: java
jdk:
  - openjdk7
  - oraclejdk7
  - oraclejdk8

script: "mvn --show-version --errors --batch-mode -Prun-its clean verify -Dittest.debug=true -Dinvoker.streamLogs=true -Dinvoker.test=jaxwscommons-43"

cache:
    directories:
    - $HOME/.m2
branches:
    except:
        - gh-pages
notifications:
    email:
        - hboutemy@apache.org
